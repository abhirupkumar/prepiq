import React from 'react';

interface PageProps {
    params: {
        questionId: string
    }
}

const Page = async ({ params }: PageProps) => {
    return (
        <div>

        </div>
    )
}

export default Page;