import React from 'react'

interface PageProps {
    params: {
        interviewId: string
    }
}

const Page = async ({ params }: PageProps) => {
    return (
        <div>

        </div>
    )
}

export default Page;